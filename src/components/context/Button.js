export default function Button({themeColor,textColor}){

    return(
        <div>
            <button>Button component - and theme is - {themeColor} and text - {textColor}</button>
        </div>
    )
  }